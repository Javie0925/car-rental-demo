server:
  port: 9098
  servlet:
    context-path: /

spring:
  datasource:
    name: test
    url: jdbc:mysql://127.0.0.1:3306/car_rental?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  mvc:
    view:
      suffix: .html

mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.example.test.bean
